{
  "name": "quote-api",
  "version": "1.0.0",
  "description": "a wen application to generate random quotes from API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Swagat",
  "license": "ISC",
  "dependencies": {
    "requests": "^0.3.0"
  }
}
